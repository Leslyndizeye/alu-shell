#!/usr/bin/env bash
# This script displays the PID and process name of processes containing the word "bash."

# Use pgrep to find and display PIDs of processes containing the word "bash"
ps aux | grep '[b]ash' | awk '{print "For PID: "$2", Full Process Name: "$11}'
